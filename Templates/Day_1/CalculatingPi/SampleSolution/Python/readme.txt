Here are two different sample solutions.  

pi_square_parallel.py distributes the array indices in a round robin 
     fashion onto the tasks. 

pi_square_parallel_blocked.py creates continuous block of array indices, 
     which get distributed onto the processors.
